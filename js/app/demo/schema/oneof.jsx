'use strict';

define(['react'], function(React) {
    return React.createClass({
        displayName: 'Type',
        render: function() {
            return <span>OneOf</span>;
        }
    });
});
