'use strict';

define(['react'], function(React) {
    return React.className({
        displayName: 'Array',
        render: function() {
            return <span><input type="text" /> plus</span>;
        }
    });
});
