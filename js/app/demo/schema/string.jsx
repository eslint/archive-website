'use strict';

define(['react'], function(React) {
    return React.createClass({
        displayName: 'String',
        render: function() {
            return <input type="text" />;
        }
    });
});
